# TripWallet

Let's go!!

## Steps
1. `$ docker-compose run --service-ports tripwallet`
2. `$ cd backend`

### Run backend
`cd backend`
`$ ./runserver.sh`

### Run frontend
`$ cd frontend`
`$ ./runserver.sh`
